# Railway config for banking-system-api (backend)
# Root Directory in Railway Dashboard must be EMPTY so this file and Dockerfile are found

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
